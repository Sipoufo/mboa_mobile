import 'package:flutter/material.dart';

/// Brand seed colours. Each app builds its theme from its own seed so the two
/// products stay visually distinct while sharing the same design system.
/// Placeholders pending the brandbook (Doc 06).
class MboaColors {
  const MboaColors._();

  /// App Mboa (general public).
  static const Color userSeed = Color(0xFF1B6B4C);

  /// App Mboa Pro (providers & agents).
  static const Color proSeed = Color(0xFF0D3B66);
}
